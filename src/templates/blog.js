import React from "react"
import { graphql, useStaticQuery } from "gatsby"
import Layout from "../components/Layout"

const Blog = () => {
  return <Layout>Blog template</Layout>
}

export default Blog
